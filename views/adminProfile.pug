doctype html
html(lang="es")
  head
    meta(charset="UTF-8")
    title Admin Profile - GoalBridge
    link(rel="stylesheet", href="/css/style.css")

  //- Incluye el encabezado (header) y el pie de página (footer)
  include header
  include layout

  //- Contenido principal de la página admin
  block content
    .container
      h1 Administrador - Panel de control

      //- Muestra los datos del administrador logueado
      .admin-info
        //p Nombre: #{admin.nombre}
        //p Correo electrónico: #{admin.email}
      
      //- Botones para redirigir a otras secciones
      .buttons
        a(href="/dashboard", class="btn btn-primary") Dashboard
        a(href="/users", class="btn btn-secondary") Users

  //- Incluye el pie de página (footer)
  include footer

  //- Scripts
  script(src="index.js")
  script(src="/js/script.js")
